demo_test:
  tracker: "strongsort"
  detection_model_path: "weights/yolov8s-seg.pt"
  reid_model_path: "weights/osnet_x0_25_msmt17.pt"
  save_video_path: "./demo.avi"
  save_video: true
  save_video_path: "./demo_out.avi"
  save_video_fps: 0
  save_video_codec: "XVID"
  # 新增：缩小输入与显示
  input_width: 960       # 可调，先试 960
  input_height: 540
  resize_ratio: 0.5      # 检测前缩放 50%（越小越省算力）
  display_ratio: 0.5     # 只缩放显示窗口大小
  # 如果只关心画面某块区域，可启用 ROI（否则删除/注释掉）
  # roi:
  #   x: 0
  #   y: 200
  #   w: 1920
  #   h: 880